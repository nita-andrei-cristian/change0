# Change

#### Welcome to the development of the app change. 
> What does it do? Well imagine this scenario:

The app tries to create a context of a user, let's say `user A`.
We now want develop a local AI system that based on `user A`s data can compute a task.

#### What's here so far?

A basic logic for allocating memory for a user graph.
Exporting and Importing of that graph.

More coming soon
